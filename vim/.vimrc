" Disable compatibility
set nocompatible

" Always encode in UTF-8
set encoding=utf8

" Activate rainbow parens (This needs to go in here)
let g:rainbow_active=1

" Load pathogen
execute pathogen#infect()
